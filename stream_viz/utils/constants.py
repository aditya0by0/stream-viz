# Data File Local constants
import os

# The below constants are for developers only, for testing purpose
_MISSING_DATA_PATH = os.path.join(os.getcwd(), r"data/cfpdss_m0.5.csv")
_NORMAL_DATA_PATH = os.path.join(os.getcwd(), r"data/cfpdss.csv")
_LEARNING_STRATEGY_DATA_PATH = os.path.join(os.getcwd(), r"data/experiment.csv")
